#!/usr/bin/env python3

print("Input 2 number(sides of right triangle) to get square of triangle\n");

first_side = float(input("first side: "))
second_side = float(input("second side: "))
square = first_side * second_side / 2

print("Square of triangle is %s" % square);